import React, { Component, PropTypes } from 'react';

export default class About extends Component {
  render () {
    return (
      <li className="demo">
        this is about page.
      </li>
    );
  }
}
